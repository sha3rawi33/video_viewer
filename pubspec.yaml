name: video_viewer
description: Multiplatform minimalist video viewer with spectacular user experience.
homepage: https://github.com/sha3rawi33/video_viewer
version: 1.2.7

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  gesture_x_detector: ^1.1.1
  helpers: ^1.1.4
  http: ^0.13.4
  path_provider: ^2.0.11
  provider: ^6.0.3
  video_player: ^2.4.5
  volume_watcher: ^2.0.1
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter

